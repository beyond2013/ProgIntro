#include <stdio.h>
#include <ctype.h>

int main(void)
{
	//int grade=65;
	char grade;
	 for(grade=1000; grade < 2000; grade++)
	{
		printf("\n value of grade is %c, int equivalent %d", grade, grade); 
	}

}
